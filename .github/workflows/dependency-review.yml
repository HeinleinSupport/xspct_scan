# SPDX-License-Identifier: EUPL-1.2
# SPDX-FileCopyrightText: 2026 Carsten Rosenberg <c.rosenberg@heinlein-support.de>

name: Dependency Review

on:
  pull_request:

jobs:
  review:
    name: Review Dependencies
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - uses: actions/checkout@v4

      - name: Dependency Review
        uses: actions/dependency-review-action@v4
        with:
          # Block PRs that introduce dependencies with known CVEs at these levels.
          fail-on-severity: moderate
          # Deny licenses that are incompatible with EUPL-1.2 or restrict
          # commercial use. Only valid SPDX identifiers are accepted here;
          # Commons-Clause and proprietary/unknown licenses have no SPDX id
          # and cannot be matched by this list. Adjust as required.
          deny-licenses: >-
            AGPL-1.0-only,
            AGPL-1.0-or-later,
            AGPL-3.0-only,
            AGPL-3.0-or-later,
            GPL-2.0-only,
            GPL-2.0-or-later,
            GPL-3.0-only,
            GPL-3.0-or-later,
            LGPL-2.0-only,
            LGPL-2.0-or-later,
            LGPL-2.1-only,
            LGPL-2.1-or-later,
            LGPL-3.0-only,
            LGPL-3.0-or-later,
            BUSL-1.1,
            SSPL-1.0
          # Post a summary comment on the PR for visibility.
          comment-summary-in-pr: always
